# =========================
# AI Business Process Auditor
# Конфигурация окружения (пример)
# =========================

# Режимы:
# - offline: локальная/заглушка (без внешних API)
# - api: запросы к API-модели
LLM_MODE=api

# Провайдер (на будущее, если добавим альтернативы)
# openai | other
LLM_PROVIDER=openai

# ---- OpenAI ----
# Ключ храните ТОЛЬКО в .env (не коммитьте)
OPENAI_API_KEY=__PUT_YOUR_KEY_HERE__

# Базовый URL (по умолчанию OpenAI API)
OPENAI_BASE_URL=https://api.openai.com/v1

# Модель по умолчанию (можно менять)
# Для бюджетного старта обычно достаточно "gpt-4o-mini"
OPENAI_MODEL=gpt-4o-mini

# Таймауты/повторы
OPENAI_TIMEOUT_SEC=60
OPENAI_MAX_RETRIES=2

# Лимиты генерации (если используем в клиенте)
OPENAI_MAX_OUTPUT_TOKENS=800
OPENAI_TEMPERATURE=0.2
